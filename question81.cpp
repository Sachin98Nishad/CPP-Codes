#include<iostream>
#include<vector>
using namespace std;

/*
Application of Vector : Vectors are the same as the dynamic arrays with the ability to 
resize itself automatically when an element is deleted or inserted.
*/
 int main(){
    vector<int>v1;   // vector of integers.
     vector<float>v2;  // vector of floats.
      vector<double>v3;   // vector of double.
       vector<string>v4;    // vector of string.
        vector<bool>v5;       // vector of boolian.
        return 0;
 }
